# 复习规则

题目、调度状态、历史日期与作答结果统一保存在 [题库](reviews/question-bank.md)，当前学习进度见 [STATUS](STATUS.md)。

## 复习范围

- 以 [学习计划](LEARNING_PLAN.md) 为边界，只将实际学过的知识、已有独立产出和实验中暴露的问题整理为题卡；记录课程及证据来源，不因计划列出某主题就视为已经学习。
- 当前保留 Day 1 的所有权与生命周期基础复习：返回结果与局部对象、按值返回与悬空关系、引用捕获的生命周期。这些 C++ 场景只用于巩固基础，不扩展成语言标准、移动语义或完整 Lambda 专项。
- 随课程推进，结合机制解释、前提与边界判断、代码错误定位及 lab 结果分析补充题卡；理论复习和实验复盘都要有实际学习依据。
- 复习用于巩固，不以所有旧题通过作为 R0 或后续课程的开课门槛。一次未通过的题按调度规则留待复习，不无限延长本轮来反复考到通过。

## 每次正式学习前

1. 筛选 `active` 且 `next_review` 不晚于实际会话日期的卡；最多 5 题。
2. 优先处理：上次未通过 → 部分通过 → 逾期最久 → 其他到期题。
3. 一次只出一道题，不提前展示答案；等待独立回答后校正。
   后续尽量使用具体代码和执行过程提问，明确对象、调用处与语句含义。区分题意理解偏差与机制错误；听过解释只记录为教学帮助，下次复习用独立作答验证，不据此判为掌握。
   同一知识点的后续复习使用新的题面，改变代码结构、对象关系、调用场景或推理角度，检验原理迁移，避免仅替换变量名或数值。仍限定在已学范围内，不用陌生语法或额外知识增加难度。题卡 ID 跟踪知识点，换题不新增重复卡或重置间隔；一次只出一道，作答前不提示答案。
4. 记录结果、日期、间隔和错误；完成本轮复习或用户明确停止后进入新内容。未处理的卡继续到期。
5. 本轮结束后按需更新 STATUS 简短摘要；题库保留调度与下次待验证点，不逐题复制到日志。仅整理计划时不启动复习。

## 每个学习小节结束后

1. 先由用户不依赖提示、用自己的话描述刚学到的内容；描述应尽量包含核心结论、原因、适用前提和边界。AI 不先给出标准答案，也不以“已阅读”直接判定掌握。
2. 根据独立描述判断当前证据属于看过、能够解释，还是仍有关键缺口；必要时一次只用一个新的具体代码、执行过程或应用场景追问，以检验理解能否迁移。
3. 听过讲解、照着材料复述或在提示后答对，只算相应的学习帮助，不自动视为独立掌握。实现和正确性分析仍需对应的独立实验或推理证据。
4. 笔记不保存用户的详细原话、问答过程或教学评语，只整理精简且准确的课程知识、用户已经表达清楚的个人理解，以及接续所需的证据结论。

## 间隔与判定

间隔为 **1、3、7、14、30、60、120 天**，从实际复习日计算，不因逾期自动完成。

| 结果 | 判定 | 调度 |
|---|---|---|
| 通过 | 无提示给出结论、原因与必要边界 | `interval_step` 从 0 开始，首次通过用 1 天，之后进入下一间隔 |
| 部分通过 | 方向正确，缺少关键机制或边界 | 不推进间隔，下一学习日再问 |
| 未通过 | 结论错误、依赖提示或无法解释 | 间隔重置，下一学习日再问 |

| 状态 | 含义 |
|---|---|
| active | 已启用，参与到期筛选 |
| parked | 尚未系统学习的断点，按实际学过的范围再拆卡 |
| suspended | 因范围调整暂停；保留题目、日期、间隔与结果，不算完成 |

暂停原因与日期单独记录，不写入作答结果；只有后续明确启用或当前练习确有需要时才考虑恢复。已明确取消且超出当前范围的专项从当前题库剔除，调整原因记入日志，历史内容从 Git 查询，不继续列为待补清单。后续课程确有相关需要时，按当时范围和实际学习重新建卡，不自动恢复整套旧题。

复习结果与调度状态不是掌握层级，采用 [统一证据标准](AGENTS.md)。答对解释题不能替代独立实现、证明或正确性/性能分析；一次答错也不自动降低整个主题层级。
