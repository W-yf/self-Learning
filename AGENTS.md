# 协作规则

本仓库是专业基础回顾的主仓库，负责总计划与总进度；各课程或阶段使用独立仓库管理学习成果。学习范围以 [LEARNING_PLAN.md](LEARNING_PLAN.md) 为准，用户最新明确要求优先于旧计划。

## 会话分工

用户暂定将主仓库管理会话作为统一管理入口，负责：

- 开启和管理独立课程仓库，明确课程范围、状态入口、远程地址及仓库索引。
- 维护宏观学习计划、课程优先级、时间安排和阶段衔接，根据实际证据汇总总进度。
- 更新主仓库文档，核对各仓库的发布状态，标记尚未提交、推送或回写的内容。
- 在开启课程、切换会话或更换电脑时，按需要使用 handoff skill 生成交接，注明基线提交、未同步内容和接手任务。

课程学习会话负责该课程的理论学习、练习、实验、调试、验证和知识笔记；一门课程可按主题或实验分为多个会话，成果仍保存在同一个课程仓库。管理会话读取课程文档与证据后回写总览，不凭聊天描述提高掌握层级。

新会话按用户指定的管理或课程学习角色接手，并读取对应仓库的状态和协作规则；本节的管理职责只适用于管理会话，不改变课程学习会话的职责。两类会话均遵守下文的记录、同步和 Git 规则。

## 读取与维护

1. 每次新学习会话先检查本地修改、当前分支与上游，拉取主仓库及当前课程仓库的远程更新，再读主仓库 README、STATUS 的 `last_updated`、`current_day`、`current_stage` 和近期任务。优先快进拉取；有未同步改动或分叉时先核对处理，保留已有内容，不静默覆盖。未能拉取时明确说明同步未核验。
2. 规划或调整范围时读 LEARNING_PLAN、RESOURCES 和最新日期日志。
3. 正式学习前读 REVIEW、题库及相关笔记、练习；一次只提出一道到期题，等待回答后再校正。
4. 主仓库 STATUS 是全局进度、课程仓库索引与交接入口；LEARNING_PLAN 只维护范围、顺序与验收，RESOURCES 只维护资料。课程仓库保存本课程详细状态与证据，handoff 文档保存有版本依据的接手快照，不另建与主计划竞争的全局路线或进度副本。
5. 学习时不逐题修改文件；完成一个小节后整理课程知识笔记，更新必要进度与下一步。本轮结束或交接时回写主仓库 STATUS 的简短摘要与成果链接；日志仅按需记录环境、同步及重大计划变更，不保存详细学习过程。统一复习队列仍在主仓库维护。只做文档整理时不推进学习日、掌握层级或复习间隔。
6. 两台电脑与不同对话通过已同步文档交接，不依赖聊天记忆。主仓库已发布文档决定全局计划与汇总进度，课程仓库已发布文档和代码提供本课程事实证据；本地未提交或未推送修改明确标注。交接快照或聊天与最新文档冲突时核对版本并明确指出，不能把课程摘要尚未回写当作课程成果不存在。
7. 历史日志、旧诊断及 Day 1 文件中的后续安排仅说明当时情况，不能恢复已取消的专项任务。初始安全检查、commit、push、远程核对及用户确认已完成，不因重构重新开启初始化流程。

## 课程仓库与交接

- 每个实际启动的课程或阶段独立管理；创建后在主仓库 STATUS 登记范围、地址、分支、状态文档、最新交接入口和已核对的成果提交。课程仓库链接回主仓库，保留课程版本和本课程验收要求。不要把计划中的仓库写成已经创建。
- 默认通过 Git 接续：新对话或换电脑后拉取主仓库和当前课程仓库，再读主仓库 STATUS 与课程 README；这两个入口保存必要进度、成果链接、下一项任务与未同步事项，不依赖聊天记忆或临时交接文件。链接使用仓库地址或相对路径，不依赖某台电脑的私人路径。
- 只有用户另行要求时才使用指定 handoff skill 生成补充快照，按实际结果登记入口；skill 不可用时如实说明。临时文件不会随 Git 同步，不作为默认接续依赖；快照不能覆盖更新的仓库状态，也不代表学习完成。
- 学习结束时先审阅并同步课程成果，再将课程已核对的提交和进度摘要写回、同步主仓库；逐仓库核对远程结果。当前会话能访问两仓库且已有同步授权时可一并处理，无需用户切换管理会话。由用户执行推送，或由助手在已获授权范围内执行；已获授权无需重复确认。任一步未完成都要记录待同步位置，不将两仓库写成全部已同步。

## 掌握与证据

统一使用以下四级，不另造等级：

| 层级 | 证据要求 |
|---|---|
| 1. 看过 | 实际阅读或接触记录 |
| 2. 能够解释 | 独立说明机制、前提与边界或反例 |
| 3. 能够独立实现 | 独立完成并验证代码、证明、推导或计算 |
| 4. 能够分析正确性与性能 | 分析不变量、复杂度或误差；适用时给出可信测量 |

只有存在相应证据才提高层级。区分用户独立产出、提示后完成和 AI 生成内容；自述经历、课程清单与单次答对不等于完整掌握。复习调度状态不是掌握层级。

## 笔记与实验

- 新课程知识笔记、实验代码与测试放在对应课程仓库，主仓库保留计划、必要进度、统一复习与成果链接。现有历史成果不因采用多仓库方式自动迁移或删除。
- 笔记只记录课程知识和用户对知识的理解，保持精简、准确，关键结论依据课程原文或官方资料，必要时附边界、例子与来源。不保存问答流水、作答原文、提示过程或教学评语，不把 AI 补充冒充用户的个人理解。小节结束后整理，课程结束时统一核对、去重和精简；进度和复习只保留接续所需摘要。代码实验保留必要的环境描述、构建/运行方法、输入来源与验证结果。
- 只配置当前练习必需的工具。C++ 练习检查生命周期、所有权和失效规则；数学题不强制编程或计时。
- 性能实验先验证正确性，再记录编译配置、数据规模、预热、重复次数与计时范围，不凭一次测量下结论。未来启用 CUDA 时补充线程层次、内存路径、同步与错误检查。
- 只使用独立编写的内容、合成数据或许可明确的公开数据；公开数据记录来源和许可。受限课程框架、测试、trace、二进制及解答在独立非公开空间使用，公开记录不能还原受限答案。

## 公开边界与 Git

- 只将允许公开的代码、资料和数据写入本仓库；不索取或复制非公开材料和公开许可不明的内容。
- 不处理或提交密码、令牌、Cookie、密钥、证书、连接串和环境变量值等凭据；公开文件不含私人绝对路径、用户名、设备名或可识别环境信息。误提供时不复制，并提醒移除或轮换。
- 被忽略目录和非公开课程空间同样遵守隐私与授权边界；不要先复制敏感资料再尝试脱敏。
- 修改前后检查 `git status --short`，保留无关改动。提交前逐文件审阅实际变更和暂存区，检查文本、文件名、二进制、大文件及疑似凭据，并运行 `git diff --check`；不能只依赖 `.gitignore`。
- 未获明确授权不执行 `git add`、`git commit`、`git push`；不擅改远程、强推、重写历史或执行破坏性清理。授权推送后核对远程分支与提交。
