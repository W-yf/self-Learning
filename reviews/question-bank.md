# 间隔复习题库

```yaml
last_updated: 2026-09-10
schedule: ../REVIEW.md
question_mode: one_at_a_time
```

## 字段说明

- `status`：`active` 表示进入复习队列；`parked` 表示完整主题尚未系统学习，只记录断点；`suspended` 表示原已启用卡因当前学习范围调整暂停调度，保留历史记录。只将 `active` 卡纳入到期筛选；这些状态不是掌握层级，暂停不表示已完成或已通过。
- `interval_step`：`0` 表示尚未首次通过；通过后依次使用 1、3、7、14、30、60、120 天。
- `next_review`：下一次最早应复习的日期；`—` 表示尚未启用。
- `last_result`：只使用 `not_reviewed`、`passed`、`partial`、`failed`。
- 答案要点供复习结束后校正，不应在提问时提前展示。

## 当前队列

| ID | 主题 | 来源 | 状态 | interval_step | last_reviewed | next_review | last_result |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| RV-D1-001 | 返回流程的时间顺序 | Day 1 巩固易混点 | active | 0 | 2026-09-10 | 2026-09-11 | failed |
| RV-D1-003 | 按值返回与悬空观察者 | Day 1 核心证据 | active | 0 | 2026-09-10 | 2026-09-11 | failed |
| RV-D1-006 | Lambda 引用捕获的生命周期 | Day 1 迁移证据 | active | 0 | 2026-09-10 | 2026-09-11 | partial |

## 2026-09-10 复习范围清理

按用户要求剔除与当前路线无直接关系的 C++ 专项题和候选清单；清理原因见 [当日日志](../logs/2026-09-10.md)，旧题及调度记录可从 Git 历史查询。移出题库不表示作答通过或已经掌握，不再作为当前待复习或待补课任务。

保留以下三个 Day 1 基础点，范围以 [学习计划](../LEARNING_PLAN.md) 为准：

- RV-D1-003：所有权与悬空关系，直接衔接 R0 的返回值所有权和接口契约。
- RV-D1-001：简短巩固返回结果与局部对象的生命周期，不展开复制消除或标准版本专项。
- RV-D1-006：以引用捕获为例检查生命周期迁移，不展开完整 Lambda 或 `std::function` 专项。

三张保留卡的题目、日期、间隔与作答结果均未改变。它们不是 CSAPP/OS 的完整先修清单，复习通过不能替代课程实验。后续只根据实际学习及实验中的问题新增相关题卡，不提前把未学主题排入复习队列。

## 2026-09-10 复习作答

- RV-D1-001：独立作答为 E-C-F-B-A-D（E 初始化返回结果对象，C 计算 return 表达式，F 函数结束并交回控制权，B 销毁函数局部对象，A 调用方使用结果，D 结果对象最终销毁）。将表达式求值与结果初始化、局部对象销毁与函数交回控制权两组顺序颠倒，且未说明结果对象销毁的具体生命周期边界，判为 failed。
- AI 已校正为 C-E-B-F-A-D，并说明结果对象在调用方对应生命周期终点销毁；校正内容不计作独立回答。间隔重置为 0，最早复习日期为 2026-09-11，在下一学习日处理，本轮不重复考到通过。
- 用户随后澄清：原作答可能包含题意理解偏差，把“初始化返回结果对象”理解为“额外创建临时对象”，也不清楚“控制权回到调用方”指程序回到调用处继续执行。因此不能仅凭该排序断言两处错误都源于生命周期机制不理解。用户报告侧对话已用代码解释，属于已接受教学帮助，尚无新的独立作答证据；保留原作答、failed 判定及调度，不提高掌握层级。
- RV-D1-001 下次复习使用具体函数、调用语句和输出位置，让用户独立追踪 return 表达式求值、接收返回值的对象初始化、局部对象销毁及调用处后续语句执行，并指出结果对象何时销毁。明确题设中的对象与语句含义，不要求推断额外临时对象，也不扩展复制消除专项；作答后再核对，不提前展示顺序。
- RV-D1-003：以 make_value 按值返回局部字符串、make_ref 返回局部字符串的 const 引用，以及 main 中的 a、b 为具体场景。用户独立判断 a 安全，认为 a 保存新的副本；对 b 不确定，并认为优化可能让 make_ref 的 local 活到 main 结束。还认为 make_value 的 local 已析构后才传出临时对象。正确识别了 a 的安全性和 b 的引用身份，但返回引用的生命周期结论错误，返回结果初始化与局部析构的顺序仍需澄清，判为 failed。
- RV-D1-003 教学反馈：按值返回先初始化结果，再销毁独立局部对象；不保证发生拷贝或额外临时对象。若发生 NRVO，make_value 的 local 与结果可为同一对象，该情形不适用于返回引用的 make_ref。make_ref 的 local 在该函数退出时销毁，b 悬空，优化不能使这种访问安全。反馈属于 AI 解释，未计为用户独立掌握；interval_step 重置为 0，next_review 为 2026-09-11，下一学习日用具体代码验证，不扩大为优化专项。
- RV-D1-006：用户独立判断不安全并给出将 `[&text]` 改为 `[text]` 的正确修复，明确表示不知道不安全的原因。方向与修复正确，缺少生命周期机制解释，判为 partial；interval_step 保持 0，next_review 为 2026-09-11，下一学习日复习。
- RV-D1-006 教学反馈：返回的可调用对象能继续存在，但引用捕获指向的函数局部 text 在函数退出时销毁；后续调用访问该对象会悬空。按值捕获在 Lambda 创建时保存字符串副本，后续由可调用对象持有并使用。解释来自 AI，不计为独立掌握；下次用具体代码检查闭包与被捕获对象各自的生命周期。
- 本轮三题已结束：RV-D1-001、RV-D1-003 为 failed，RV-D1-006 为 partial；均留待下一学习日验证，不重复考到通过。进入 R0-01 最小环境与起点任务。

## RV-D1-001：返回流程的时间顺序

**问题**

不考虑异常，函数按值返回且没有 NRVO 时，请按先后顺序说明：计算 `return` 表达式、初始化返回结果对象、销毁函数局部对象、函数结束、调用方使用结果、结果对象最终销毁。

**答案要点（回答后核对）**

1. 计算 `return` 表达式；
2. 初始化函数调用的结果对象；
3. 销毁 `return` 表达式相关的临时对象，然后按规则销毁函数局部对象；
4. 被调用函数结束，控制权回到调用方；
5. 调用方继续使用结果；
6. 结果对象在调用方决定的生命周期终点销毁。它不是函数退出时销毁的普通局部对象。

**常见错误**

- 认为返回结果对象会在函数结束前被销毁；
- 把“结果已经初始化”误说成“调用方已经继续执行”；
- 忽略 NRVO 时局部对象与结果对象可能是同一个对象。

## RV-D1-003：按值返回与悬空观察者

**问题**

为什么返回局部 `std::string` 的值通常安全，而返回该局部对象的引用或地址不安全？回答时指出谁拥有独立对象、谁只是观察者，以及局部对象何时结束生命周期。

**答案要点（回答后核对）**

- 按值返回会初始化一个在调用表达式中继续存活的结果对象，可能复制、移动或直接构造。
- 引用和裸指针通常只是非拥有观察者，不会延长局部对象生命周期。
- 普通局部对象在函数退出过程结束生命周期；继续通过引用或地址访问会悬空并产生未定义行为。

## RV-D1-006：Lambda 引用捕获的生命周期

**问题**

下面代码为什么不安全？只修改捕获方式并保持 `std::function<std::string()>` 的无参数调用签名，应如何修复？

```cpp
std::function<std::string()> make_reader()
{
    std::string text{"sample"};
    return [&text] { return text; };
}
```

**答案要点（回答后核对）**

- 返回的闭包对象可以继续存在，但局部 `text` 在函数退出时结束生命周期。
- `[&text]` 只保存非拥有引用关系，随后悬空。
- 改为 `[text] { return text; }`，闭包对象在 Lambda 表达式求值时保存独立副本。
