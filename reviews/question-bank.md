# 间隔复习题库

```yaml
last_updated: 2026-09-09
schedule: ../REVIEW.md
question_mode: one_at_a_time
```

## 字段说明

- `status`：`active` 表示进入复习队列；`parked` 表示完整主题尚未系统学习，只记录断点；`suspended` 表示原已启用卡因当前学习范围调整暂停调度，保留历史记录。只将 `active` 卡纳入到期筛选；这些状态不是掌握层级，暂停不表示已完成或已通过。
- `interval_step`：`0` 表示尚未首次通过；通过后依次使用 1、3、7、14、30、60、120 天。
- `next_review`：下一次最早应复习的日期；`—` 表示尚未启用。
- `last_result`：只使用 `not_reviewed`、`passed`、`partial`、`failed`。
- 答案要点供复习结束后校正，不应在提问时提前展示。

## 当前队列

| ID | 主题 | 来源 | 状态 | interval_step | last_reviewed | next_review | last_result |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| RV-D1-001 | 返回流程的时间顺序 | Day 1 巩固易混点 | active | 0 | — | 2026-08-13 | not_reviewed |
| RV-D1-002 | C++17 保证复制消除与 NRVO | Day 1 巩固易混点 | suspended | 0 | — | 2026-08-13 | not_reviewed |
| RV-D1-003 | 按值返回与悬空观察者 | Day 1 核心证据 | active | 0 | — | 2026-08-14 | not_reviewed |
| RV-D1-004 | C++11/14 未消除时的移动次数 | Day 1 巩固版本语境 | suspended | 0 | — | 2026-08-14 | not_reviewed |
| RV-D1-005 | 六个特殊成员函数 | 新暴露的易漏项 | suspended | 0 | — | 2026-08-15 | not_reviewed |
| RV-D1-006 | Lambda 引用捕获的生命周期 | Day 1 迁移证据 | active | 0 | — | 2026-08-15 | not_reviewed |
| GAP-CPP-001 | 完整移动语义 | 旧专项学习要求，现按需候选 | parked | 0 | — | — | not_reviewed |
| GAP-CPP-002 | Lambda 闭包与 `std::function` | Day 1 断点，现按需候选 | parked | 0 | — | — | not_reviewed |

## 2026-09-09 调度范围调整

用户将当前路线固定为专业基础回顾，取消 C++ 标准与专项必学要求。因此 RV-D1-002、RV-D1-004、RV-D1-005 暂停，RV-D1-001、RV-D1-003、RV-D1-006 保留为通用生命周期基础复习。原 6 张到期卡中 3 张暂停，截至本日仍有 3 张 `active` 卡到期。

这次调整只改变调度范围，未提问或作答；全部卡片的 `next_review`、`last_reviewed`、`interval_step` 和 `last_result` 保持原值。暂停不是作答结果或完成证据，恢复时按实际会话日期与学习需要重新检查，不因经过时间自动推进间隔。

两个 `GAP-CPP` 主题是保留的旧专项候选，没有新增掌握结论。后续只有明确启用专项或基础练习确有需求时才选取相关内容，不能继续作为专业基础路线的整套前置要求。

## RV-D1-001：返回流程的时间顺序

**问题**

不考虑异常，函数按值返回且没有 NRVO 时，请按先后顺序说明：计算 `return` 表达式、初始化返回结果对象、销毁函数局部对象、函数结束、调用方使用结果、结果对象最终销毁。

**答案要点（回答后核对）**

1. 计算 `return` 表达式；
2. 初始化函数调用的结果对象；
3. 销毁 `return` 表达式相关的临时对象，然后按规则销毁函数局部对象；
4. 被调用函数结束，控制权回到调用方；
5. 调用方继续使用结果；
6. 结果对象在调用方决定的生命周期终点销毁。它不是函数退出时销毁的普通局部对象。

**常见错误**

- 认为返回结果对象会在函数结束前被销毁；
- 把“结果已经初始化”误说成“调用方已经继续执行”；
- 忽略 NRVO 时局部对象与结果对象可能是同一个对象。

## RV-D1-002：C++17 保证复制消除与 NRVO

**问题**

比较下面两种返回形式。在 C++17 中，哪一种保证不需要从返回表达式复制/移动，哪一种仍依赖可选 NRVO？若 NRVO 发生，默认构造函数是否还会执行？

```cpp
Person make_prvalue()
{
    return Person{};
}

Person make_named()
{
    Person local;
    return local;
}
```

**答案要点（回答后核对）**

- `return Person{};` 属于 C++17 保证的直接构造情形，不先创建一个独立临时对象再移动。
- `return local;` 是 NRVO 候选，C++17 仍不强制编译器实施 NRVO。
- NRVO 发生时，`local` 直接构造在结果对象的存储位置；默认构造仍执行一次，只消除了复制/移动。
- NRVO 未发生且移动可用时，从 `local` 移动构造结果对象。

## RV-D1-003：按值返回与悬空观察者

**问题**

为什么返回局部 `std::string` 的值通常安全，而返回该局部对象的引用或地址不安全？回答时指出谁拥有独立对象、谁只是观察者，以及局部对象何时结束生命周期。

**答案要点（回答后核对）**

- 按值返回会初始化一个在调用表达式中继续存活的结果对象，可能复制、移动或直接构造。
- 引用和裸指针通常只是非拥有观察者，不会延长局部对象生命周期。
- 普通局部对象在函数退出过程结束生命周期；继续通过引用或地址访问会悬空并产生未定义行为。

## RV-D1-004：C++11/14 未消除时的移动次数

**问题**

假设 `Person` 可移动、不是 `const`，并且在 C++11/14 中完全不进行复制消除：

```cpp
Person create()
{
    Person a;
    return a;
}

Person b{create()};
```

分别会调用多少次默认构造、移动构造和拷贝构造？C++17 中为什么至多只剩一次移动？

**答案要点（回答后核对）**

- C++11/14 完全不消除：默认构造一次，移动构造两次，拷贝构造零次。
- 第一次从 `a` 构造函数结果，第二次从结果临时对象构造 `b`。
- C++17 中，函数调用产生的纯右值直接初始化 `b`，不再有“结果临时对象再移动到 `b`”这一步；`return a;` 未 NRVO 时仍可能有从 `a` 到结果的一次移动。

## RV-D1-005：六个特殊成员函数

**问题**

请列出 C++ 类的六类特殊成员函数，并用一小段代码区分“拷贝构造”和“拷贝赋值”。为什么不能说每个类一定自动获得六个可用实现？

**答案要点（回答后核对）**

- 默认构造、析构、拷贝构造、拷贝赋值、移动构造、移动赋值。
- `Person b{a};` 是用已有 `a` 创建新对象 `b`，属于拷贝构造；先创建 `b` 再执行 `b = a;` 属于拷贝赋值。
- 用户声明某些构造或析构/复制/移动操作会影响其他特殊成员的隐式声明；成员不可复制或不可移动时，相应函数还可能被定义为删除。

## RV-D1-006：Lambda 引用捕获的生命周期

**问题**

下面代码为什么不安全？只修改捕获方式并保持 `std::function<std::string()>` 的无参数调用签名，应如何修复？

```cpp
std::function<std::string()> make_reader()
{
    std::string text{"sample"};
    return [&text] { return text; };
}
```

**答案要点（回答后核对）**

- 返回的闭包对象可以继续存在，但局部 `text` 在函数退出时结束生命周期。
- `[&text]` 只保存非拥有引用关系，随后悬空。
- 改为 `[text] { return text; }`，闭包对象在 Lambda 表达式求值时保存独立副本。

## GAP-CPP-001：完整移动语义（暂不出题）

**启用条件**

后续明确启用移动语义专项，或基础练习确有相关需求时，按实际学过的内容拆分题卡；当前不要求进入完整专项。

**旧专项候选范围（按需选择，不要求全部完成）**

- 左值、右值、值类别与 `T&&`；
- 移动构造和移动赋值的语义；
- `std::move` 只是转换，不自行移动资源；
- 特殊成员函数的隐式生成、删除和抑制规则；
- 移动后对象“有效但状态未指定”；
- Rule of Zero、Rule of Five；
- `noexcept` 与容器重分配；
- RVO、NRVO 和移动语义的关系；
- 自行编写的合成资源类及构造、移动、析构证据。

当前只记录：移动构造函数、移动赋值和右值引用由 C++11 引入；尚未系统学习，不分配新的掌握结论。

## GAP-CPP-002：Lambda 闭包与 `std::function`（暂不出题）

**启用条件**

后续明确启用 Lambda 专项，或基础练习确有相关需求时，按实际学过的内容拆分题卡；当前不要求进入完整专项。

**旧专项候选范围（按需选择，不要求全部完成）**

- Lambda 表达式产生闭包类型和闭包对象；
- `auto`、`decltype` 与不可直接拼写的闭包类型；
- 按值、按引用、初始化捕获和捕获生命周期；
- 不同 Lambda 表达式具有不同闭包类型；
- 模板直接接收闭包与 `std::function` 类型擦除的差异和成本。
